pub fn run() {
    // let Some(x) = some_option_value; // `let` bindings require an "irrefutable pattern", pattern `None` not covered
    // if let Some(x) = some_option_value {
    //     println!("{}", x);
    // }
    // if let x = 5 {
    //     // this pattern will always match, so the `if let` is useless
    //     println!("{}", x);
    // };
}
